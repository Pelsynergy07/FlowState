# Bump on every release, in lockstep with the GitHub release tag (vX.Y.Z)
# and packaging/installer.iss's MyAppVersion -- this is what the running
# app compares against GitHub Releases to decide whether an update exists.
__version__ = "0.1.4"
